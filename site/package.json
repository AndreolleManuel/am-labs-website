{
  "name": "site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js tests/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
